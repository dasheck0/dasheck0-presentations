import type { ThemeConfig } from '@shared/types';

export const nanoverseTheme: ThemeConfig = {
  name: 'nanoverse',
  reveal: {
    controls: true,
    progress: true,
    center: true,
    hash: true,
    transition: 'fade',
    slideNumber: true,
    showSlideNumber: 'all',
    controlsLayout: 'bottom-right',
  },
};
