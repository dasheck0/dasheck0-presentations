import type { ThemeConfig } from '@shared/types';

export const nanogiantsTheme: ThemeConfig = {
  name: 'nanogiants',
  reveal: {
    controls: true,
    progress: true,
    center: true,
    hash: true,
    transition: 'slide',
    slideNumber: true,
    showSlideNumber: 'all',
    controlsLayout: 'bottom-right',
  },
};
